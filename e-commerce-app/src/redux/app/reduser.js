
const initState = {
   data:[],
   loading:false,
   error:false
    
  };
  
  export const appReduser = (state=initState, { type, payload }) => {
    switch (type) {
      
      default:
        return state;
    }
  };