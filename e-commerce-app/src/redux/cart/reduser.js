
const initState = {
    data:[],
    loading:false,
    error:false
     
   };
   
   export const cartReduser = (state=initState, { type, payload }) => {
     switch (type) {
       
       default:
         return state;
     }
   };